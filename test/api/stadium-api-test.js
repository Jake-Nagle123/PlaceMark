import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { eventService } from "./event-service.js";
import { kevin, trip, testEvents, testStadiums, gameday } from "../fixtures.js";

suite("Stadium API tests", () => {
  let user = null;
  let weekOne = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("create stadium", async () => {
  });

  test("create Multiple stadiums", async () => {
  });

  test("Delete stadium", async () => {
  });

  test("test denormalised stadium", async () => {
  });
});